import { createAction, props } from '@ngrx/store';

// export const InitHome = createAction(
//   '[InitHome] InitHome',
  
// );

// export const StoreCart = createAction(
//   '[Store Cart] Store Cart',
//   props<{ cart }>()
// );
